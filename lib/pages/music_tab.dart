import 'package:flutter/material.dart';

class MusicTabPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "MusicTabPage Sayfası İçeriği",
        style: TextStyle(color: Colors.white, fontSize: 24),
      ),
    );
  }
}



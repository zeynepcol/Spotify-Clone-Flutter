import 'package:flutter/material.dart';

class PodcastTabPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "PodcastTabPage Sayfası İçeriği",
        style: TextStyle(color: Colors.white, fontSize: 24),
      ),
    );
  }
}